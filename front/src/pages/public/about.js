import navLinks from "../../components/navLinks.js";

const about = `<div>
                    ${navLinks("Products")}
                    ${navLinks("About")}
                    ${navLinks("Company")}
                </div>`;

export default about;

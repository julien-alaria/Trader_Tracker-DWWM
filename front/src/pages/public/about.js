import navLink from "../../components/navLinks/navLink.js";

const about = `<div>
                    ${navLink("/", "Home")}
                    ${navLink("/register", "Register")}
                    ${navLink("/login", "Login")}
                </div>`;

export default about;
